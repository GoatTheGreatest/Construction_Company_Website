# Rise Solution Co.

This is a code bundle for Rise Solution Co., a premium integrated services website for Saudi Arabia. The original design is based on a luxury corporate website UI.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
